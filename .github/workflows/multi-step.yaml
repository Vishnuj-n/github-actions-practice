name: Multi-step workflow

on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "step 1"

      - name: make a empty file
        run: touch one.txt

      - name: remove it
        run: rm one.txt
